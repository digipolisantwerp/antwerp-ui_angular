describe('Notifications Component Test', () => {
  it('dummy', () => {
    expect(1).toBe(1);
    /*
    We cannot unit test the notifications component itself because it is only
    an extension of the ngx-toastr Toast component. We rely on the accuracy of
    the testing of this package itself.
     */
  });
});
