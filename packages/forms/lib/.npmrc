# Set the default prefix for packages to "none".
# ~		Matches the most recent patch version
# 		e.g. : ~2.4.1 means it will check for 2.4.x where x is anything
#			---
# ^		Matches the most recent minor version
# 		e.g. : ^2.4.1 means it will check for 2.x.x where x is anything
save-prefix=""
