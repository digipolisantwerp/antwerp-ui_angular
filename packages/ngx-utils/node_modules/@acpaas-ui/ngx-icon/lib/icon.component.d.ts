import { OnInit, Renderer2 } from '@angular/core';
export declare class IconComponent implements OnInit {
    private renderer;
    readonly classes: string;
    name: string;
    ariaLabel: string;
    className: string;
    constructor(renderer: Renderer2);
    ngOnInit(): void;
    private fetchAntwerpIcons;
}
